<h1>Current Regions:</h1>

<ul>
  <% @regions.each do |region| %>
    <li><%= link_to region.name, region_path(region) %></li>
  <% end %>
</ul>

<hr>

<%= link_to "Add a region", new_region_path %>

<%= link_to "Animals", animals_path %>
