<h1>Animals found in the forest</h1>

<ul>
  <% @animals.each do |animal| %>
    <li><%= link_to animal.name, animal_path(animal) %></li>
  <% end %>
</ul>

<hr>

<%= link_to "Add an animal", new_animal_path %>

<%= link_to "Regions", regions_path %>
